# 🚀 Test Hiển Thị Markdown

Chào mừng bạn đến với trình kiểm tra hiển thị! File này chứa các thành phần phổ biến để bạn test CSS và Plugin.

---

## 1. Định dạng văn bản
Bạn có thể viết chữ **in đậm**, *in nghiêng*, hoặc ~~gạch ngang~~. jhjshdsfj

> Đây là một đoạn trích dẫn (Blockquote) dùng để nhấn mạnh các thông tin quan trọng.

---

## 2. Danh sách và Tasklist
- [x] Cài đặt `react-markdown`
- [x] Thêm plugin `remark-gfm` để hiển thị bảng
- [ ] Tùy chỉnh CSS cho đẹp hơn

### Danh sách có thứ tự:
1. Bước một: Import thư viện.
2. Bước hai: Fetch dữ liệu.
3. Bước ba: Render.

---

## 3. Hiển thị Code
Nếu bạn đã cài đặt `react-syntax-highlighter`, đoạn code dưới đây sẽ có màu sắc rất đẹp:

```javascript
function HelloWorld() {
  console.log("Xin chào từ React Markdown!");
  return <h1>Hello World</h1>;
}